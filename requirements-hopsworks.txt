# Hopsworks is intentionally separate because its dependency tree is large.
# Python 3.11 is recommended for the Hopsworks environment.
# Hopsworks dependencies
hopsworks[python]==5.0.6
protobuf>=4.25.4,<5